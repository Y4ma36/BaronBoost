import React from "react";
import styled from "styled-components";

const PolicyWrapper = styled.div`
  height: 80vh;
  background: black;
`;

const Policy = () => {
  return <PolicyWrapper>Policy</PolicyWrapper>;
};

export default Policy;
